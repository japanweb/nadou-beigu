<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.keyuan.mapper.ShopTypeMapper">
    <select id="selectTypeByName" resultType="java.lang.Long">
        select id from shop_type where name = #{shopType}
    </select>
</mapper>